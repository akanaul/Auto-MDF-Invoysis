# Normalize all text files and enforce LF for source files
* text=auto

# Python and shell scripts should use LF in the repository
*.py text eol=lf
*.sh text eol=lf

# Batch files on Windows should keep CRLF in worktree
*.bat text eol=crlf

# Markdown and text files: normalize text but preserve platform line endings
*.md text
*.txt text
